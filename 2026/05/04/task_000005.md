# task_000005 — run_tests

- date: 2026-05-04 12:06 (Europe/Kyiv)
- duration: 7m 30s
- status: done
- branch: develop
- output_branch: —
- runs: 1

## what was done

Запустив `flutter test` на гілці `develop` (1750 тестів).

## artifacts

- `/tmp/flutter-test.log` — повний вивід тестів

## notes

**passed: 1750 / failed: 2 / skipped: 0**

---

**Falling tests (2):**

**1.** `verifyCode success path loader stays active after verifyCode (cleared by screen dispose via clearStore)`
- File: `test/features/auth/email_verification/store/email_verification_store_test.dart:241`
- Error:
  ```
  Bad state: GetIt: Object/factory with type MarketingSubscriptionStore is not registered inside GetIt.
  package:jetwallet/features/auth/email_verification/store/email_verification_store.dart 235:19
  ```

**2.** `regression: infinite loader bug loader stays active after verifyCode — clearStore is the safety net`
- File: `test/features/auth/email_verification/store/email_verification_store_test.dart:327`
- Identical error: `MarketingSubscriptionStore` not registered in GetIt mock setup.

---

**Root cause:** `email_verification_store.dart:235` (додано в одному з останніх коммітів з маркетинговими змінами) тепер звертається до `getIt<MarketingSubscriptionStore>()` всередині `verifyCode`, але тест-mock-setup не реєструє цей store в GetIt. Потрібно додати `MarketingSubscriptionStore` до mock-реєстрацій у setUp/setUpAll відповідного тест-файлу.
